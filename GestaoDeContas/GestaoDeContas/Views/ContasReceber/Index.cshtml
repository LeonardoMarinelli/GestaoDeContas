@model IEnumerable<GestaoDeContas.Models.ContaReceber>

<div class="container mt-4">
    <h2 class="text-center mb-4">Lista de Contas a Receber</h2>

    <a href="/ContasReceber/Create" class="btn btn-success mb-3">Adicionar Novo Recebimento</a>

    <table class="table table-striped table-hover">
        <thead class="table-dark">
        <tr>
            <th>Descrição</th>
            <th>Valor</th>
            <th>Data de Recebimento</th>
            <th>Status</th>
            <th>Categoria</th>
            <th>Ações</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>@item.Descricao</td>
            <td>R$ @item.Valor.ToString("F2")</td>
            <td>@item.DataRecebimento.ToShortDateString()</td>
            <td>
                <span class="badge @(item.Status ? "bg-success" : "bg-warning")">
                    @(item.Status ? "Recebida" : "Pendente")
                </span>
            </td>
            <td>@item.Categoria</td>
            <td>
                <a href="/ContasReceber/Edit/@item.Id" class="btn btn-warning btn-sm">
                    <i class="bi bi-pencil-square"></i>Editar
                </a>
                <a href="/ContasReceber/Delete/@item.Id" class="btn btn-danger btn-sm">
                    <i class="bi bi-trash"></i>Excluir
                </a>
            </td>
        </tr>
        }
        </tbody>
    </table>
</div>